import 'package:file_encryptify/ui/app.dart';
import 'package:logger/logger.dart';

// the entrance of the app
void main() {
  // run the app
  Logger().d('main() function executed');
  launchFileEncryptifyApp();
}
